{
  "name": "solito-expo-router",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@types/react": "^18.0.17",
    "@types/react-native": "^0.69.5",
    "eslint": "^8.21.0",
    "turbo": "^1.4.2",
    "typescript": "^4.7.4"
  },
  "scripts": {
    "native": "cd apps/expo && expo start",
    "native-cline-cache": "cd apps/expo && expo start -c",
    "web": "cd apps/next && yarn next",
    "web-clean-cache": "cd apps/next && yarn cache clean && yarn next ",
    "refresh-project": "del node_modules && cd apps/next && del node_modules && cd ../expo && del node_modules && cd ../../packages/app && yarn cache clean && yarn && del %localappdata%/Temp/haste-map-* && del %localappdata%/Temp/metro-cache"
  },
  "resolutions": {
    "metro": "^0.73.1",
    "metro-resolver": "^0.73.1"
  },
  "nohoist": [
    "**/expo-router",
    "**/expo-router/**"
  ],
  "engines": {
    "node": "^16.0.0"
  }
}
